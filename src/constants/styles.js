import { WHITE } from './colors';
import { DEFAULT_HORIZONTAL_SEPARATION } from './dimensions';

export const container = {
  flex: 1,
  alignSelf: 'stretch',
  paddingHorizontal: DEFAULT_HORIZONTAL_SEPARATION,
  backgroundColor: WHITE,
};
